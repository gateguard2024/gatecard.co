/**
 * Kept as a re-export so nothing that imported this path breaks. The demo data
 * now lives in ./properties, which carries three properties rather than one.
 */
export { PROPERTIES, DEMO_NOTES, getMoveInContext } from './properties'
